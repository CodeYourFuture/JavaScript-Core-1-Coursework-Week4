{
  
    numbers.map(number=>number.reduce((total,number)=>total+number > 16 ? ))
}
function containsNumber(numbers) {
    return /[0-9]/.test(numbers)
    }
function lengthOfArray(numbers){
   return (numbers.toString().lenght === 16)
}
function sumNumbers(numbers){
    return numbers.reduce((total,number)=>number + total)
    function reapedItem(numbers){
        return numbers.split("").parseInt( numbers.every((value, index) => {
            return numbers.indexOf(value) === index))
    }
}
function validatorNumbers(numbers){
    if(containsNumber(numbers) && lengthOfArray(numbera) && sumNumbers(numbera) >16 &&  ) 
}