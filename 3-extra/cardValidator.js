function validateCardNumb() {
  //input = number 16 digits long
  // let regex = /^\d{16}$/;
  // let evenTest = (numberTostring.length - 1 === charAt(-1) % 2) === 0;
  // nuumbers.reduce() > 16;
  // //return boolean value
  // return Bool;
}
